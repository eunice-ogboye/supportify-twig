{% extends "base.twig" %}

{% block title %}Welcome to Supportly{% endblock %}

{% block styles %}
<style>
    .hero {
        padding: 100px 0 150px;
        text-align: center;
        position: relative;
        overflow: hidden;
    }

    .hero::before {
        content: '';
        position: absolute;
        top: 50px;
        left: -100px;
        width: 300px;
        height: 300px;
        background: #dbeafe;
        border-radius: 50%;
        z-index: -1;
    }

    .hero::after {
        content: '';
        position: absolute;
        bottom: -50px;
        left: 0;
        right: 0;
        height: 200px;
        background: linear-gradient(180deg, #0ea5e9 0%, #06b6d4 100%);
        border-radius: 50% 50% 0 0 / 100% 100% 0 0;
        z-index: -1;
    }

    .hero h1 {
        font-size: 48px;
        margin-bottom: 20px;
        color: #1e293b;
    }

    .hero h1 .highlight {
        color: #0ea5e9;
    }

    .hero p {
        font-size: 18px;
        color: #64748b;
        max-width: 700px;
        margin: 0 auto 40px;
        line-height: 1.6;
    }

    .hero-buttons {
        display: flex;
        gap: 20px;
        justify-content: center;
        align-items: center;
    }

    .features {
        padding: 80px 0;
        background: white;
    }

    .features h2 {
        text-align: center;
        font-size: 36px;
        margin-bottom: 60px;
        color: #1e293b;
    }

    .features-grid {
        display: grid;
        grid-template-columns: repeat(auto-fit, minmax(300px, 1fr));
        gap: 40px;
        max-width: 1100px;
        margin: 0 auto;
    }

    .feature-card {
        background: #f8fafc;
        padding: 40px 30px;
        border-radius: 12px;
        text-align: center;
    }

    .feature-icon {
        font-size: 32px;
        margin-bottom: 20px;
    }

    .feature-card h3 {
        font-size: 22px;
        margin-bottom: 15px;
        color: #1e293b;
        display: flex;
        align-items: center;
        justify-content: center;
        gap: 10px;
    }

    .feature-card h3 .icon {
        color: #0ea5e9;
    }

    .feature-card p {
        color: #64748b;
        line-height: 1.6;
    }
</style>
{% endblock %}

{% block content %}
<section class="hero">
    <div class="container">
        <h1>Welcome to <span class="highlight">Supportly</span></h1>
        <p>Manage your support tickets seamlessly — track, resolve, and stay organized all in one beautiful dashboard.</p>
        <div class="hero-buttons">
            <a href="index.php?page=login" class="btn btn-primary">Login</a>
            <a href="index.php?page=register" class="btn btn-outline">Get Started</a>
        </div>
    </div>
</section>

<section class="features">
    <div class="container">
        <h2>Why Choose Supportly?</h2>
        <div class="features-grid">
            <div class="feature-card">
                <h3><span class="icon">🔒</span> Secure Access</h3>
                <p>Your account and ticket data are safely stored with session-based protection.</p>
            </div>
            <div class="feature-card">
                <h3><span class="icon">⚡</span> Fast Management</h3>
                <p>Create, view, edit, and resolve tickets in seconds with smooth, real-time updates.</p>
            </div>
            <div class="feature-card">
                <h3><span class="icon">📊</span> Smart Dashboard</h3>
                <p>Get insights at a glance — open tickets, resolved issues, and progress tracking.</p>
            </div>
        </div>
    </div>
</section>

<footer class="footer">
    <div class="container">
        <p>© 2025 Supportly. All Rights Reserved.</p>
    </div>
</footer>
{% endblock %}